[project]
name = "uol-project"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = ["pytest>=9.0.3", "fastapi"]

[dependency-groups]
dev = ["icecream>=2.2.0"]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
